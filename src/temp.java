
public class temp {

	
	Node temp = FirstNode
			
			while (temp != Null)
				
				print(temp.Data); 
	temp = temp.Next; 
	
	Node{
		object Data; 
		Node Next;
	}
	
	
	Node newNode = new Node (Data); 
	FirstNode = newNode; 
	
	Node FirstNOde = Null //should check this to see if a list is empty if(FirstNode == Null)
			
			FirstNode.next = newNode; 
	
	//Delete node
	Node DeletedNode
	Node prevNode
	Node NextNode; 
	
	NextNode = DeletedNode.next
	prevNode.Next = NextNode; 
	Deleted.next = Null; 
	
	//algorithm
	//assumne exponents are sorted
	//compare exponents
			
	}
}
